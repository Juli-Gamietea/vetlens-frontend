import { Text, View } from 'react-native';

export const Bobo = ({ navigation }) => {
    return(
        <View style={{flex: 1, flexDirection: 'column', justifyContent: 'center', alignItems: 'center'}}>
            <Text>Hola, pantalla de prueba</Text>
        </View>
    )
}